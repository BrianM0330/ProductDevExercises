import * as React from "react";

interface ICalculatorProps {}

const Calculator: React.FunctionComponent<ICalculatorProps> = (props) => {
  return <div> calculator </div>;
};

export default Calculator;
